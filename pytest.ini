[pytest]
# Use pytest-asyncio's auto mode to pick a compatible backend
asyncio_mode = auto

# Set the default fixture loop scope explicitly to avoid deprecation warnings
asyncio_default_fixture_loop_scope = function

# Common patterns for test discovery (optional)
python_files = tests/test_*.py tests/*_test.py