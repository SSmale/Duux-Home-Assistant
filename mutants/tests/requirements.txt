# Dependencies for running the test suite (tests/).
#
# Deliberately NOT using pytest-homeassistant-custom-component here: it pulls
# in a very large, fast-moving dependency tree pinned to a specific
# Home Assistant core release, and is overkill for unit-testing a single
# custom component. Instead, the tests import the real entity base classes
# from a plain `homeassistant` install and exercise them with small, explicit
# fakes for `hass`/`coordinator` (see tests/conftest.py). This keeps the
# suite fast and far less prone to breaking on unrelated HA core changes.
homeassistant==2025.1.4
pytest>=8.0,<10
pytest-asyncio>=0.24,<2
pytest-cov>=5.0
requests>=2.31.0

