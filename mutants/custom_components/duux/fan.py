"""Platform for DUUX fan integration."""

from __future__ import annotations

import logging
from typing import Any, Iterator

from homeassistant.components.fan import (
    FanEntity,
    FanEntityFeature,
)
from homeassistant.config_entries import ConfigEntry
from homeassistant.core import HomeAssistant
from homeassistant.helpers.entity_platform import AddEntitiesCallback
from homeassistant.helpers.update_coordinator import CoordinatorEntity
from homeassistant.util.percentage import (
    ordered_list_item_to_percentage,
    percentage_to_ordered_list_item,
    percentage_to_ranged_value,
    ranged_value_to_percentage,
)

from .const import (
    DOMAIN,
    DUUX_DTID_AIR_PURIFIER,
    DUUX_FAN_TYPES,
    DUUX_DTID_FAN,
    DUUX_STID_WHISPER_FLEX,
    DUUX_STID_WHISPER_FLEX_2,
    DUUX_STID_BRIGHT_2,
    DUUX_STID_WHISPER_FLEX_ELIVATE,
    DUUX_STID_WHISPER_FLEX_ULTIMATE,
)


_LOGGER = logging.getLogger(__name__)

# Define preset modes
PRESET_MODE_NORMAL = "normal"
PRESET_MODE_NATURAL = "natural"
PRESET_MODE_NIGHT = "night"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_async_setup_entry__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_async_setup_entry__mutmut)
async def async_setup_entry(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_orig(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_1(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = None
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_2(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = None
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_3(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["XXapiXX"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_4(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["API"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_5(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = None
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_6(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["XXcoordinatorsXX"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_7(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["COORDINATORS"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_8(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = None

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_9(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["XXdevicesXX"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_10(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["DEVICES"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_11(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = None
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_12(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = None
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_13(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") and {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_14(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get(None) or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_15(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("XXsensorTypeXX") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_16(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensortype") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_17(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("SENSORTYPE") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_18(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = None
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_19(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get(None)
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_20(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("XXtypeXX")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_21(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("TYPE")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_22(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = None
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_23(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") and ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_24(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get(None) or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_25(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("XXgoogleDeviceTypeXX") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_26(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googledevicetype") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_27(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("GOOGLEDEVICETYPE") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_28(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or "XXXX"
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_29(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = None  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_30(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(None)[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_31(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split("XX.XX")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_32(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[+1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_33(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-2] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_34(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else "XXXX"
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_35(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = None
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_36(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get(None)
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_37(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("XXsensorTypeIdXX")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_38(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensortypeid")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_39(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("SENSORTYPEID")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_40(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = None
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_41(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get(None)
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_42(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("XXdeviceIdXX")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_43(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceid")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_44(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("DEVICEID")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_45(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = None

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_46(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(None)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_47(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is not None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_48(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            break

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_49(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = None

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_50(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get(None, "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_51(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", None)

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_52(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_53(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", )

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_54(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("XXnameXX", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_55(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("NAME", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_56(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "XXUnknownXX")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_57(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_58(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "UNKNOWN")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_59(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_60(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word not in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_61(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    None,
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_62(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    None,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_63(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    None,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_64(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    None,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_65(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    None,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_66(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_67(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_68(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_69(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_70(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_71(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "XXDevice '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not XX"
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_72(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "device '%s' (type=%s, sensortypeid=%s, googletype=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_73(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "DEVICE '%S' (TYPE=%S, SENSORTYPEID=%S, GOOGLETYPE=%S) IS NOT "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_74(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "XXofficially categorised as a fan but matches a known fan Google XX"
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_75(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_76(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "OFFICIALLY CATEGORISED AS A FAN BUT MATCHES A KNOWN FAN GOOGLE "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_77(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "XXtype — attempting to set up as fan. Please report this to the XX"
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_78(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_79(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "TYPE — ATTEMPTING TO SET UP AS FAN. PLEASE REPORT THIS TO THE "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_80(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "XXintegration developer.XX",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_81(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "INTEGRATION DEVELOPER.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_82(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                break

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_83(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id != DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_84(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(None)
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_85(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(None, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_86(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, None, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_87(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, None))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_88(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_89(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_90(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, ))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_91(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id != DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_92(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(None)
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_93(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(None, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_94(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, None, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_95(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, None))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_96(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_97(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_98(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, ))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_99(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id != DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_100(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(None)
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_101(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(None, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_102(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, None, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_103(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, None))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_104(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_105(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_106(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, ))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_107(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id != DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_108(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(None)
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_109(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(None, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_110(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, None, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_111(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, None))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_112(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_113(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_114(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, ))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_115(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id != DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_116(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(None)
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_117(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(None, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_118(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, None, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_119(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, None))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_120(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_121(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_122(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, ))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_123(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(None)
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_124(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(None, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_125(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, None, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_126(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, None))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_127(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_128(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_129(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, ))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_130(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                None,
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_131(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                None,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_132(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                None,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_133(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_134(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_135(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_136(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "XXUnknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.XX",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_137(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "unknown fan sensortypeid %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_138(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "UNKNOWN FAN SENSORTYPEID %S FOR DEVICE '%S' — USING GENERIC AUTODISCOVERY ENTITY.",
                sensor_type_id,
                model,
            )

    async_add_entities(entities)


async def x_async_setup_entry__mutmut_139(
    hass: HomeAssistant,
    entry: ConfigEntry,
    async_add_entities: AddEntitiesCallback,
) -> None:
    """Set up DUUX fan based on a config entry."""
    data = hass.data[DOMAIN][entry.entry_id]
    api = data["api"]
    coordinators = data["coordinators"]
    devices = data["devices"]

    entities = []
    for device in devices:
        sensor_type = device.get("sensorType") or {}
        device_type_id = sensor_type.get("type")
        google_type = sensor_type.get("googleDeviceType") or ""
        last_word = (
            google_type.split(".")[-1] if google_type else ""
        )  # "HEATER" OR "THERMOSTAT"
        sensor_type_id = device.get("sensorTypeId")
        device_id = device.get("deviceId")
        coordinator = coordinators.get(device_id)

        # Skip devices that have no coordinator (were filtered out in __init__)
        if coordinator is None:
            continue

        model = sensor_type.get("name", "Unknown")

        if device_type_id not in [*DUUX_DTID_FAN, *DUUX_DTID_AIR_PURIFIER]:
            if last_word in DUUX_FAN_TYPES:
                _LOGGER.warning(
                    "Device '%s' (type=%s, sensorTypeId=%s, googleType=%s) is not "
                    "officially categorised as a fan but matches a known fan Google "
                    "type — attempting to set up as fan. Please report this to the "
                    "integration developer.",
                    model,
                    device_type_id,
                    sensor_type_id,
                    google_type,
                )
            else:
                continue

        if sensor_type_id == DUUX_STID_WHISPER_FLEX_2:
            entities.append(DuuxWhisperFlexTwoFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX:
            entities.append(DuuxWhisperFlexFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_BRIGHT_2:
            entities.append(DuuxAirPurifierFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ULTIMATE:
            entities.append(DuuxWhisperFlexUltimateFan(coordinator, api, device))
        elif sensor_type_id == DUUX_STID_WHISPER_FLEX_ELIVATE:
            entities.append(DuuxWhisperFlexElevateFan(coordinator, api, device))
        else:
            entities.append(DuuxFanAutoDiscovery(coordinator, api, device))
            _LOGGER.warning(
                "Unknown fan sensorTypeId %s for device '%s' — using generic autodiscovery entity.",
                sensor_type_id,
                model,
            )

    async_add_entities(None)

mutants_x_async_setup_entry__mutmut['_mutmut_orig'] = x_async_setup_entry__mutmut_orig # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_1'] = x_async_setup_entry__mutmut_1 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_2'] = x_async_setup_entry__mutmut_2 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_3'] = x_async_setup_entry__mutmut_3 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_4'] = x_async_setup_entry__mutmut_4 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_5'] = x_async_setup_entry__mutmut_5 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_6'] = x_async_setup_entry__mutmut_6 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_7'] = x_async_setup_entry__mutmut_7 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_8'] = x_async_setup_entry__mutmut_8 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_9'] = x_async_setup_entry__mutmut_9 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_10'] = x_async_setup_entry__mutmut_10 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_11'] = x_async_setup_entry__mutmut_11 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_12'] = x_async_setup_entry__mutmut_12 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_13'] = x_async_setup_entry__mutmut_13 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_14'] = x_async_setup_entry__mutmut_14 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_15'] = x_async_setup_entry__mutmut_15 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_16'] = x_async_setup_entry__mutmut_16 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_17'] = x_async_setup_entry__mutmut_17 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_18'] = x_async_setup_entry__mutmut_18 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_19'] = x_async_setup_entry__mutmut_19 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_20'] = x_async_setup_entry__mutmut_20 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_21'] = x_async_setup_entry__mutmut_21 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_22'] = x_async_setup_entry__mutmut_22 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_23'] = x_async_setup_entry__mutmut_23 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_24'] = x_async_setup_entry__mutmut_24 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_25'] = x_async_setup_entry__mutmut_25 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_26'] = x_async_setup_entry__mutmut_26 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_27'] = x_async_setup_entry__mutmut_27 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_28'] = x_async_setup_entry__mutmut_28 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_29'] = x_async_setup_entry__mutmut_29 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_30'] = x_async_setup_entry__mutmut_30 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_31'] = x_async_setup_entry__mutmut_31 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_32'] = x_async_setup_entry__mutmut_32 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_33'] = x_async_setup_entry__mutmut_33 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_34'] = x_async_setup_entry__mutmut_34 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_35'] = x_async_setup_entry__mutmut_35 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_36'] = x_async_setup_entry__mutmut_36 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_37'] = x_async_setup_entry__mutmut_37 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_38'] = x_async_setup_entry__mutmut_38 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_39'] = x_async_setup_entry__mutmut_39 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_40'] = x_async_setup_entry__mutmut_40 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_41'] = x_async_setup_entry__mutmut_41 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_42'] = x_async_setup_entry__mutmut_42 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_43'] = x_async_setup_entry__mutmut_43 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_44'] = x_async_setup_entry__mutmut_44 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_45'] = x_async_setup_entry__mutmut_45 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_46'] = x_async_setup_entry__mutmut_46 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_47'] = x_async_setup_entry__mutmut_47 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_48'] = x_async_setup_entry__mutmut_48 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_49'] = x_async_setup_entry__mutmut_49 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_50'] = x_async_setup_entry__mutmut_50 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_51'] = x_async_setup_entry__mutmut_51 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_52'] = x_async_setup_entry__mutmut_52 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_53'] = x_async_setup_entry__mutmut_53 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_54'] = x_async_setup_entry__mutmut_54 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_55'] = x_async_setup_entry__mutmut_55 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_56'] = x_async_setup_entry__mutmut_56 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_57'] = x_async_setup_entry__mutmut_57 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_58'] = x_async_setup_entry__mutmut_58 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_59'] = x_async_setup_entry__mutmut_59 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_60'] = x_async_setup_entry__mutmut_60 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_61'] = x_async_setup_entry__mutmut_61 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_62'] = x_async_setup_entry__mutmut_62 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_63'] = x_async_setup_entry__mutmut_63 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_64'] = x_async_setup_entry__mutmut_64 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_65'] = x_async_setup_entry__mutmut_65 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_66'] = x_async_setup_entry__mutmut_66 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_67'] = x_async_setup_entry__mutmut_67 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_68'] = x_async_setup_entry__mutmut_68 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_69'] = x_async_setup_entry__mutmut_69 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_70'] = x_async_setup_entry__mutmut_70 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_71'] = x_async_setup_entry__mutmut_71 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_72'] = x_async_setup_entry__mutmut_72 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_73'] = x_async_setup_entry__mutmut_73 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_74'] = x_async_setup_entry__mutmut_74 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_75'] = x_async_setup_entry__mutmut_75 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_76'] = x_async_setup_entry__mutmut_76 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_77'] = x_async_setup_entry__mutmut_77 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_78'] = x_async_setup_entry__mutmut_78 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_79'] = x_async_setup_entry__mutmut_79 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_80'] = x_async_setup_entry__mutmut_80 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_81'] = x_async_setup_entry__mutmut_81 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_82'] = x_async_setup_entry__mutmut_82 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_83'] = x_async_setup_entry__mutmut_83 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_84'] = x_async_setup_entry__mutmut_84 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_85'] = x_async_setup_entry__mutmut_85 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_86'] = x_async_setup_entry__mutmut_86 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_87'] = x_async_setup_entry__mutmut_87 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_88'] = x_async_setup_entry__mutmut_88 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_89'] = x_async_setup_entry__mutmut_89 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_90'] = x_async_setup_entry__mutmut_90 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_91'] = x_async_setup_entry__mutmut_91 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_92'] = x_async_setup_entry__mutmut_92 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_93'] = x_async_setup_entry__mutmut_93 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_94'] = x_async_setup_entry__mutmut_94 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_95'] = x_async_setup_entry__mutmut_95 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_96'] = x_async_setup_entry__mutmut_96 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_97'] = x_async_setup_entry__mutmut_97 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_98'] = x_async_setup_entry__mutmut_98 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_99'] = x_async_setup_entry__mutmut_99 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_100'] = x_async_setup_entry__mutmut_100 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_101'] = x_async_setup_entry__mutmut_101 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_102'] = x_async_setup_entry__mutmut_102 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_103'] = x_async_setup_entry__mutmut_103 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_104'] = x_async_setup_entry__mutmut_104 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_105'] = x_async_setup_entry__mutmut_105 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_106'] = x_async_setup_entry__mutmut_106 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_107'] = x_async_setup_entry__mutmut_107 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_108'] = x_async_setup_entry__mutmut_108 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_109'] = x_async_setup_entry__mutmut_109 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_110'] = x_async_setup_entry__mutmut_110 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_111'] = x_async_setup_entry__mutmut_111 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_112'] = x_async_setup_entry__mutmut_112 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_113'] = x_async_setup_entry__mutmut_113 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_114'] = x_async_setup_entry__mutmut_114 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_115'] = x_async_setup_entry__mutmut_115 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_116'] = x_async_setup_entry__mutmut_116 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_117'] = x_async_setup_entry__mutmut_117 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_118'] = x_async_setup_entry__mutmut_118 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_119'] = x_async_setup_entry__mutmut_119 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_120'] = x_async_setup_entry__mutmut_120 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_121'] = x_async_setup_entry__mutmut_121 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_122'] = x_async_setup_entry__mutmut_122 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_123'] = x_async_setup_entry__mutmut_123 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_124'] = x_async_setup_entry__mutmut_124 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_125'] = x_async_setup_entry__mutmut_125 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_126'] = x_async_setup_entry__mutmut_126 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_127'] = x_async_setup_entry__mutmut_127 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_128'] = x_async_setup_entry__mutmut_128 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_129'] = x_async_setup_entry__mutmut_129 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_130'] = x_async_setup_entry__mutmut_130 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_131'] = x_async_setup_entry__mutmut_131 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_132'] = x_async_setup_entry__mutmut_132 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_133'] = x_async_setup_entry__mutmut_133 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_134'] = x_async_setup_entry__mutmut_134 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_135'] = x_async_setup_entry__mutmut_135 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_136'] = x_async_setup_entry__mutmut_136 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_137'] = x_async_setup_entry__mutmut_137 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_138'] = x_async_setup_entry__mutmut_138 # type: ignore # mutmut generated
mutants_x_async_setup_entry__mutmut['x_async_setup_entry__mutmut_139'] = x_async_setup_entry__mutmut_139 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanǁasync_turn_on__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanǁasync_turn_off__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanǁasync_set_percentage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut: MutantDict = {}  # type: ignore


class DuuxFan(CoordinatorEntity, FanEntity):
    """Representation of a DUUX fan."""

    @_mutmut_mutated(mutants_xǁDuuxFanǁ__init____mutmut)
    def __init__(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_orig(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_1(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(None)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_2(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = None
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_3(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = None
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_4(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = None
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_5(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["XXidXX"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_6(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["ID"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_7(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = None  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_8(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["XXdeviceIdXX"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_9(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceid"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_10(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["DEVICEID"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_11(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = None
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_12(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = None
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_13(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") and device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_14(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get(None) or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_15(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("XXdisplayNameXX") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_16(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayname") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_17(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("DISPLAYNAME") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_18(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get(None)
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_19(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("XXnameXX")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_20(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("NAME")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_21(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = None

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_22(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = False

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_23(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = None

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_24(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON & FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_25(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE & FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_26(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED & FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_27(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = None  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_28(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = None  # To be defined in subclasses
        self._attr_speed_count = 0  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_29(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = None  # To be defined in subclasses

    def xǁDuuxFanǁ__init____mutmut_30(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator)
        self._api = api
        self._device = device
        self._device_id = device["id"]
        self._device_mac = device["deviceId"]  # MAC address
        self._attr_unique_id = f"duux_{self._device_id}"
        self._attr_name = device.get("displayName") or device.get("name")
        self._attr_has_entity_name = True

        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )

        self._speed_range = []  # To be defined in subclasses
        self._attr_preset_modes = []  # To be defined in subclasses
        self._attr_speed_count = 1  # To be defined in subclasses

    @property
    def available(self) -> bool:
        """Return True if entity is available."""
        return self.coordinator.last_update_success and (
            self.coordinator.data or {}
        ).get("online", True)

    @property
    def device_info(self):
        """Return device information."""
        return {
            "identifiers": {(DOMAIN, str(self._device_id))},
            "name": self._attr_name,
            "manufacturer": self._device.get("manufacturer", "Duux"),
            "model": self._device.get("sensorType", {}).get("name", "Unknown"),
        }

    @property
    def is_on(self) -> bool:
        return (self.coordinator.data or {}).get("power") == 1

    @property
    def percentage(self) -> int | None:
        speed = (self.coordinator.data or {}).get("speed")
        if speed is None:
            return None
        try:
            return ordered_list_item_to_percentage(self._speed_range, int(speed))
        except (ValueError, KeyError):
            return None

    @property
    def preset_mode(self) -> str | None:
        """Return the current preset mode."""
        mode = (self.coordinator.data or {}).get("mode")

        if mode == 0:
            return PRESET_MODE_NORMAL
        elif mode == 1:
            return PRESET_MODE_NATURAL
        elif mode == 2:
            return PRESET_MODE_NIGHT

        return None

    @_mutmut_mutated(mutants_xǁDuuxFanǁasync_turn_on__mutmut)
    async def async_turn_on(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_orig(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_1(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            None, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_2(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, None, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_3(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, None
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_4(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_5(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_6(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_7(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_8(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = None
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_9(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = None
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_10(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["XXpowerXX"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_11(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["POWER"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_12(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_13(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(None)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_14(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_15(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(None)

        if preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_16(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is None:
            await self.async_set_preset_mode(preset_mode)

    async def xǁDuuxFanǁasync_turn_on__mutmut_17(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

        if percentage is not None:
            await self.async_set_percentage(percentage)

        if preset_mode is not None:
            await self.async_set_preset_mode(None)

    @_mutmut_mutated(mutants_xǁDuuxFanǁasync_turn_off__mutmut)
    async def async_turn_off(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_orig(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_1(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            None, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_2(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, None, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_3(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, None
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_4(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_5(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_6(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_7(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_8(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = None
        newData["power"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_9(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = None
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_10(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["XXpowerXX"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_11(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["POWER"] = False
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_12(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = True
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_turn_off__mutmut_13(self, **kwargs: Any) -> None:
        """Turn the fan off."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = False
        self.coordinator.async_set_updated_data(None)

    @_mutmut_mutated(mutants_xǁDuuxFanǁasync_set_percentage__mutmut)
    async def async_set_percentage(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_orig(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_1(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage != 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_2(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 1:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_3(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = None
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_4(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(None, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_5(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, None)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_6(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_7(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, )
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_8(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_9(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                None,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_10(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                None,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_11(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                None,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_12(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                None,
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_13(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                None,
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_14(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_15(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_16(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_17(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_18(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_19(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[1],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_20(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[+1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_21(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-2],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_22(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = None
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_23(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = None
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_24(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["XXspeedXX"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_25(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["SPEED"] = speed
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_percentage__mutmut_26(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        if speed is not None:
            await self.hass.async_add_executor_job(
                self._api.set_speed,
                self._device_mac,
                speed,
                self._speed_range[0],
                self._speed_range[-1],
            )
            newData = self.coordinator.data
            newData["speed"] = speed
            self.coordinator.async_set_updated_data(None)

    @_mutmut_mutated(mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut)
    async def async_set_preset_mode(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_orig(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_1(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = ""

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_2(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode != PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_3(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = None
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_4(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_5(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode != PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_6(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = None
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_7(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 2
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_8(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode != PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_9(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = None

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_10(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 3

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_11(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_12(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                None, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_13(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, None, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_14(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, None
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_15(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_16(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_17(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_18(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = None
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_19(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = None
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_20(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["XXmodeXX"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_21(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["MODE"] = mode_value
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanǁasync_set_preset_mode__mutmut_22(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        mode_value = None

        if preset_mode == PRESET_MODE_NORMAL:
            mode_value = 0
        elif preset_mode == PRESET_MODE_NATURAL:
            mode_value = 1
        elif preset_mode == PRESET_MODE_NIGHT:
            mode_value = 2

        if mode_value is not None:
            await self.hass.async_add_executor_job(
                self._api.set_mode, self._device_mac, mode_value
            )
            newData = self.coordinator.data
            newData["mode"] = mode_value
            self.coordinator.async_set_updated_data(None)

mutants_xǁDuuxFanǁ__init____mutmut['_mutmut_orig'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_1'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_2'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_3'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_4'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_5'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_6'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_7'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_8'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_9'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_10'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_11'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_12'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_13'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_14'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_15'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_16'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_17'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_18'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_19'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_20'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_21'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_22'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_23'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_24'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_25'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_26'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_27'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_28'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_29'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_29 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁ__init____mutmut['xǁDuuxFanǁ__init____mutmut_30'] = DuuxFan.xǁDuuxFanǁ__init____mutmut_30 # type: ignore # mutmut generated

mutants_xǁDuuxFanǁasync_turn_on__mutmut['_mutmut_orig'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_1'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_2'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_3'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_4'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_5'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_6'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_7'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_8'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_9'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_10'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_11'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_12'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_13'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_14'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_15'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_16'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_on__mutmut['xǁDuuxFanǁasync_turn_on__mutmut_17'] = DuuxFan.xǁDuuxFanǁasync_turn_on__mutmut_17 # type: ignore # mutmut generated

mutants_xǁDuuxFanǁasync_turn_off__mutmut['_mutmut_orig'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_1'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_2'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_3'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_4'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_5'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_6'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_7'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_8'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_9'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_10'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_11'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_12'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_turn_off__mutmut['xǁDuuxFanǁasync_turn_off__mutmut_13'] = DuuxFan.xǁDuuxFanǁasync_turn_off__mutmut_13 # type: ignore # mutmut generated

mutants_xǁDuuxFanǁasync_set_percentage__mutmut['_mutmut_orig'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_1'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_2'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_3'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_4'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_5'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_6'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_7'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_8'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_9'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_10'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_11'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_12'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_13'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_14'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_15'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_16'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_17'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_18'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_19'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_20'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_21'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_22'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_23'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_24'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_25'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_percentage__mutmut['xǁDuuxFanǁasync_set_percentage__mutmut_26'] = DuuxFan.xǁDuuxFanǁasync_set_percentage__mutmut_26 # type: ignore # mutmut generated

mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['_mutmut_orig'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_1'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_2'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_3'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_4'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_5'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_6'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_7'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_8'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_9'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_10'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_11'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_12'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_13'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_14'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_15'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_16'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_17'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_18'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_19'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_20'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_21'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanǁasync_set_preset_mode__mutmut['xǁDuuxFanǁasync_set_preset_mode__mutmut_22'] = DuuxFan.xǁDuuxFanǁasync_set_preset_mode__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut: MutantDict = {}  # type: ignore


class DuuxWhisperFlexTwoFan(DuuxFan):
    """Representation of a DUUX Whisper Flex 2 fan."""

    @_mutmut_mutated(mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut)
    def __init__(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_orig(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_1(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(None, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_2(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, None, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_3(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, None)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_4(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_5(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_6(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, )
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_7(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = None
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_8(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(None)
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_9(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(None, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_10(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, None))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_11(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_12(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, ))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_13(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(2, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_14(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 32))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_15(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = None
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_16(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = None
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_17(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_2 = list(range(1, 31))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_2
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = None

mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['_mutmut_orig'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_1'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_2'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_3'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_4'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_5'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_6'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_7'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_8'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_9'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_10'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_11'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_12'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_13'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_14'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_15'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_16'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexTwoFanǁ__init____mutmut['xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_17'] = DuuxWhisperFlexTwoFan.xǁDuuxWhisperFlexTwoFanǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut: MutantDict = {}  # type: ignore


class DuuxWhisperFlexElevateFan(DuuxFan):
    """Representation of a DUUX Whisper Flex Elevatefan."""

    @_mutmut_mutated(mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut)
    def __init__(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_orig(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_1(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(None, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_2(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, None, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_3(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, None)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_4(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_5(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_6(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, )
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_7(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = None
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_8(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(None)
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_9(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(None, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_10(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, None))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_11(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_12(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, ))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_13(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(2, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_14(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 28))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_15(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = None
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_16(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = None
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_17(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = None
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_18(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = None

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_19(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF & FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_20(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON & FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_21(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE & FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    def xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_22(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX_ELEVATE = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX_ELEVATE
        self._attr_preset_modes = [PRESET_MODE_NORMAL, PRESET_MODE_NATURAL]
        self._attr_speed_count = len(self._speed_range)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED & FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
            | FanEntityFeature.OSCILLATE
        )

    @property
    def oscillating(self) -> bool:
        """Is the fan oscillating."""
        return (self.coordinator.data or {}).get("horosc") == 1

    @_mutmut_mutated(mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut)
    async def async_oscillate(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_orig(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_1(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            None, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_2(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, None, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_3(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, None
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_4(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_5(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_6(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_7(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 2 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_8(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 1
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_9(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = None
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_10(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = None
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_11(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["XXhoroscXX"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_12(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["HOROSC"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_13(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 2 if oscillating else 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_14(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 1
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_15(self, oscillating: bool) -> None:
        """Oscillate the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_horosc_bool, self._device_mac, 1 if oscillating else 0
        )
        newData = self.coordinator.data
        newData["horosc"] = 1 if oscillating else 0
        self.coordinator.async_set_updated_data(None)

mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['_mutmut_orig'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_1'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_2'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_3'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_4'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_5'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_6'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_7'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_8'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_9'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_10'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_11'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_12'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_13'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_14'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_15'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_16'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_17'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_18'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_19'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_20'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_21'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁ__init____mutmut['xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_22'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁ__init____mutmut_22 # type: ignore # mutmut generated

mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['_mutmut_orig'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_1'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_2'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_3'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_4'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_5'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_6'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_7'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_8'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_9'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_10'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_11'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_12'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_13'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_14'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut['xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_15'] = DuuxWhisperFlexElevateFan.xǁDuuxWhisperFlexElevateFanǁasync_oscillate__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut: MutantDict = {}  # type: ignore


class DuuxWhisperFlexUltimateFan(DuuxFan):
    """Representation of a DUUX Whisper Flex Ultimate fan (sensorTypeId 40)."""

    @_mutmut_mutated(mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut)
    def __init__(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_orig(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_1(self, coordinator, api, device):
        super().__init__(None, api, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_2(self, coordinator, api, device):
        super().__init__(coordinator, None, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_3(self, coordinator, api, device):
        super().__init__(coordinator, api, None)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_4(self, coordinator, api, device):
        super().__init__(api, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_5(self, coordinator, api, device):
        super().__init__(coordinator, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_6(self, coordinator, api, device):
        super().__init__(coordinator, api, )

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_7(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = None
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_8(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(None)
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_9(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(None, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_10(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, None))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_11(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_12(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, ))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_13(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(2, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_14(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, 32))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_15(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = None

        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,  # value 0
            PRESET_MODE_NATURAL,  # value 1
            PRESET_MODE_NIGHT,  # value 2
        ]

    def xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_16(self, coordinator, api, device):
        super().__init__(coordinator, api, device)

        self._speed_range = list(range(1, 31))
        self._attr_speed_count = len(self._speed_range)

        self._attr_preset_modes = None

mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['_mutmut_orig'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_1'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_2'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_3'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_4'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_5'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_6'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_7'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_8'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_9'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_10'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_11'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_12'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_13'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_14'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_15'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut['xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_16'] = DuuxWhisperFlexUltimateFan.xǁDuuxWhisperFlexUltimateFanǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut: MutantDict = {}  # type: ignore


class DuuxWhisperFlexFan(DuuxFan):
    """Representation of a DUUX Whisper Flex 2 fan."""

    @_mutmut_mutated(mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut)
    def __init__(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_orig(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_1(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(None, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_2(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, None, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_3(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, None)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_4(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_5(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_6(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, )
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_7(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = None
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_8(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(None)
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_9(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(None, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_10(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, None))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_11(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_12(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, ))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_13(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(2, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_14(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 27))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_15(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = None
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_16(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = None
        self._attr_speed_count = len(self._speed_range)

    def xǁDuuxWhisperFlexFanǁ__init____mutmut_17(
        self,
        coordinator,
        api,
        device,
    ) -> None:
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        SPEED_RANGE_WHISPER_FLEX = list(range(1, 26))
        self._speed_range = SPEED_RANGE_WHISPER_FLEX
        self._attr_preset_modes = [
            PRESET_MODE_NORMAL,
            PRESET_MODE_NATURAL,
            PRESET_MODE_NIGHT,
        ]
        self._attr_speed_count = None

mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['_mutmut_orig'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_1'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_2'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_3'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_4'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_5'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_6'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_7'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_8'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_9'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_10'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_11'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_12'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_13'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_14'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_15'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_16'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxWhisperFlexFanǁ__init____mutmut['xǁDuuxWhisperFlexFanǁ__init____mutmut_17'] = DuuxWhisperFlexFan.xǁDuuxWhisperFlexFanǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut: MutantDict = {}  # type: ignore


class DuuxFanAutoDiscovery(DuuxFan):
    """Duux fan autodiscovery — builds speed list and presets from device traits."""

    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut)
    def __init__(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_orig(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_1(self, coordinator, api, device):
        super().__init__(None, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_2(self, coordinator, api, device):
        super().__init__(coordinator, None, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_3(self, coordinator, api, device):
        super().__init__(coordinator, api, None)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_4(self, coordinator, api, device):
        super().__init__(api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_5(self, coordinator, api, device):
        super().__init__(coordinator, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_6(self, coordinator, api, device):
        super().__init__(coordinator, api, )
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_7(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = None  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_8(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = None  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_9(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = None
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_10(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(None) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_11(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["XXspeedXX"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_12(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["SPEED"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_13(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["XXspeedXX"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_14(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["SPEED"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_15(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = None
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_16(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(None) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_17(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(None, max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_18(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), None)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_19(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_20(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), )) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_21(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(None), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_22(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) - 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_23(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(None) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_24(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 2)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_25(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = None

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_26(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_27(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["name"] for p in self._mode_presets] and None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_28(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["XXnameXX"] for p in self._mode_presets] or None

    def xǁDuuxFanAutoDiscoveryǁ__init____mutmut_29(self, coordinator, api, device):
        super().__init__(coordinator, api, device)
        self._speed_presets = (
            self._discover_speeds()
        )  # list of {"speed", "label", "command"}
        self._mode_presets = (
            self._discover_modes()
        )  # list of {"name", "value", "label", "command"}

        # Wire up the speed range HA needs for percentage conversion
        numeric = [int(p["speed"]) for p in self._speed_presets if p["speed"].isdigit()]
        self._speed_range = (
            list(range(min(numeric), max(numeric) + 1)) if numeric else []
        )
        self._attr_speed_count = len(self._speed_range)

        # Preset modes come from the Modes trait, not speeds
        self._attr_preset_modes = [p["NAME"] for p in self._mode_presets] or None

    # ── Trait discovery helpers ────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut)
    def _get_traits(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_orig(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_1(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = None
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_2(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") and {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_3(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get(None) or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_4(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("XXsensorTypeXX") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_5(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensortype") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_6(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("SENSORTYPE") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_7(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = None
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_8(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get(None)
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_9(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("XXTraitsXX")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_10(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_11(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("TRAITS")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_12(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(None)

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_13(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(None, "Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_14(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, None))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_15(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find("Traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_16(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, ))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_17(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "XXTraitsXX"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_18(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "traits"))

    # ── Trait discovery helpers ────────────────────────────────────────────────

    def xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_19(self) -> list:
        """Return the Traits list from the device's sensorType."""
        sensor_type: dict = self._device.get("sensorType") or {}
        traits = sensor_type.get("Traits")
        if isinstance(traits, list):
            return traits
        # Fallback deep-search (handles unexpected nesting)
        return list(DuuxFanAutoDiscovery._deep_find(self._device, "TRAITS"))

    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut)
    def _discover_speeds(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_orig(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_1(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = None

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_2(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = None

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_3(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            None,
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_4(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_5(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_6(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict) or (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_7(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed" and (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_8(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get(None) == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_9(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("XXnameXX") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_10(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("NAME") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_11(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") != "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_12(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "XXFanSpeedXX"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_13(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "fanspeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_14(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FANSPEED"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_15(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith(None)
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_16(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get(None, "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_17(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", None)
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_18(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_19(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", )
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_20(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") and {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_21(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get(None) or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_22(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("XXconfigXX") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_23(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("CONFIG") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_24(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("XXunique_nameXX", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_25(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("UNIQUE_NAME", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_26(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "XXXX")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_27(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("XXFanSpeedXX")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_28(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("fanspeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_29(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FANSPEED")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_30(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is not None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_31(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug(None, self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_32(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", None)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_33(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug(self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_34(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", )
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_35(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("XX%s: no FanSpeed trait foundXX", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_36(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no fanspeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_37(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%S: NO FANSPEED TRAIT FOUND", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_38(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = None
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_39(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") and []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_40(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get(None) or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_41(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("XXcommandsXX") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_42(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("COMMANDS") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_43(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = None

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_44(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[1] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_45(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "XXtune set speed {fanSpeed}XX"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_46(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanspeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_47(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "TUNE SET SPEED {FANSPEED}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_48(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = None
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_49(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") and {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_50(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get(None) or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_51(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("XXsettingsXX") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_52(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("SETTINGS") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_53(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = None

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_54(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) and []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_55(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            None
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_56(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") and {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_57(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get(None) or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_58(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("XXavailableFanSpeedsXX") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_59(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availablefanspeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_60(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("AVAILABLEFANSPEEDS") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_61(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "XXspeedsXX"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_62(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "SPEEDS"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_63(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = None
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_64(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_65(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                break
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_66(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = None
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_67(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") and ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_68(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") and raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_69(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get(None) or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_70(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("XXspeed_nameXX") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_71(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("SPEED_NAME") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_72(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get(None) or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_73(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("XXspeedNameXX") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_74(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedname") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_75(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("SPEEDNAME") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_76(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or "XXXX"
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_77(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_78(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                break

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_79(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = None
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_80(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") and []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_81(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get(None) or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_82(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("XXspeed_valuesXX") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_83(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("SPEED_VALUES") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_84(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) or sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_85(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get(None) == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_86(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("XXlangXX") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_87(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("LANG") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_88(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") != "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_89(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "XXenXX":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_90(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "EN":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_91(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = None
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_92(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") and []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_93(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get(None) or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_94(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("XXspeed_synonymXX") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_95(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("SPEED_SYNONYM") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_96(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = None
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_97(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[1]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_98(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    return

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_99(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                None
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_100(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "XXspeedXX": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_101(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "SPEED": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_102(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "XXlabelXX": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_103(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "LABEL": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_104(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "XXcommandXX": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_105(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "COMMAND": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_106(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace(None, speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_107(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", None),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_108(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace(speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_109(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", ),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_110(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("XX{fanSpeed}XX", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_111(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanspeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_112(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{FANSPEED}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_113(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug(None, self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_114(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", None, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_115(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, None)
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_116(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug(self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_117(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_118(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%s: discovered %d speeds", self._attr_name, )
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_119(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("XX%s: discovered %d speedsXX", self._attr_name, len(speeds))
        return speeds

    def xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_120(self) -> list[dict]:
        """Parse the FanSpeed trait into a list of speed dicts."""
        traits = self._get_traits()

        fan_speed_trait = next(
            (
                t
                for t in traits
                if isinstance(t, dict)
                and (
                    t.get("name") == "FanSpeed"
                    or (t.get("config") or {})
                    .get("unique_name", "")
                    .startswith("FanSpeed")
                )
            ),
            None,
        )

        if fan_speed_trait is None:
            _LOGGER.debug("%s: no FanSpeed trait found", self._attr_name)
            return []

        commands: list = fan_speed_trait.get("commands") or []
        cmd_template: str = commands[0] if commands else "tune set speed {fanSpeed}"

        settings: dict = fan_speed_trait.get("settings") or {}
        raw_speeds: list = (settings.get("availableFanSpeeds") or {}).get(
            "speeds"
        ) or []

        speeds = []
        for raw in raw_speeds:
            if not isinstance(raw, dict):
                continue
            speed_name: str = raw.get("speed_name") or raw.get("speedName") or ""
            if not speed_name:
                continue

            label = speed_name
            for sv in raw.get("speed_values") or []:
                if isinstance(sv, dict) and sv.get("lang") == "en":
                    synonyms = sv.get("speed_synonym") or []
                    if synonyms:
                        label = synonyms[0]
                    break

            speeds.append(
                {
                    "speed": speed_name,
                    "label": label,
                    "command": cmd_template.replace("{fanSpeed}", speed_name),
                }
            )

        _LOGGER.debug("%S: DISCOVERED %D SPEEDS", self._attr_name, len(speeds))
        return speeds

    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut)
    def _discover_modes(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_orig(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_1(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = None
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_2(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = None

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_3(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) and trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_4(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_5(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get(None) != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_6(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("XXnameXX") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_7(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("NAME") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_8(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") == "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_9(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "XXModesXX":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_10(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_11(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "MODES":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_12(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                break

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_13(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = None
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_14(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") and []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_15(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get(None) or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_16(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("XXcommandsXX") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_17(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("COMMANDS") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_18(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = None
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_19(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[1] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_20(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else "XXXX"
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_21(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = None
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_22(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") and {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_23(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get(None) or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_24(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("XXconfigXX") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_25(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("CONFIG") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_26(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = None

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_27(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") and {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_28(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get(None) or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_29(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("XXsettingsXX") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_30(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("SETTINGS") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_31(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") and []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_32(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get(None) or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_33(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("XXavailableModesXX") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_34(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availablemodes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_35(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("AVAILABLEMODES") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_36(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_37(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    break

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_38(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = None

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_39(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") and ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_40(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") and mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_41(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get(None) or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_42(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("XXcommand_keyXX") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_43(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("COMMAND_KEY") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_44(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get(None) or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_45(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("XXcommandKeyXX") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_46(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandkey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_47(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("COMMANDKEY") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_48(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or "XXXX"
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_49(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") and []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_50(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get(None) or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_51(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("XXsettingsXX") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_52(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("SETTINGS") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_53(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_54(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        break

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_55(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = None
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_56(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName") and ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_57(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name") and raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_58(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get(None)
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_59(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("XXsetting_nameXX")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_60(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("SETTING_NAME")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_61(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get(None)
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_62(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("XXsettingNameXX")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_63(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingname")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_64(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("SETTINGNAME")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_65(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or "XXXX"
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_66(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = None

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_67(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue") and ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_68(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value") and raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_69(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get(None)
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_70(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("XXsetting_valueXX")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_71(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("SETTING_VALUE")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_72(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get(None)
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_73(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("XXsettingValueXX")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_74(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingvalue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_75(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("SETTINGVALUE")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_76(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or "XXXX"
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_77(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = None
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_78(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") and []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_79(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get(None) or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_80(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("XXsetting_valuesXX") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_81(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("SETTING_VALUES") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_82(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) or sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_83(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get(None) == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_84(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("XXlangXX") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_85(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("LANG") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_86(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") != "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_87(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "XXenXX":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_88(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "EN":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_89(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = None
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_90(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") and []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_91(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get(None) or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_92(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("XXsetting_synonymXX") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_93(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("SETTING_SYNONYM") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_94(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = None
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_95(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[1]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_96(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            return

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_97(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = None

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_98(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(None, setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_99(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", None)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_100(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_101(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", )

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_102(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        None
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_103(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "XXnameXX": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_104(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "NAME": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_105(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "XXsetting_nameXX": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_106(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "SETTING_NAME": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_107(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "XXvalueXX": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_108(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "VALUE": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_109(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "XXcommandXX": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_110(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "COMMAND": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_111(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug(None, self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_112(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", None, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_113(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, None)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_114(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug(self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_115(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_116(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%s: discovered mode presets: %s", self._attr_name, )
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_117(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("XX%s: discovered mode presets: %sXX", self._attr_name, presets)
        return presets

    def xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_118(self) -> list[dict]:
        """Parse all Modes traits into a flat list of preset dicts."""
        traits = self._get_traits()
        presets: list[dict] = []

        for trait in traits:
            if not isinstance(trait, dict) or trait.get("name") != "Modes":
                continue

            commands: list = trait.get("commands") or []
            cmd_template: str = commands[0] if commands else ""
            config: dict = trait.get("config") or {}
            settings: dict = trait.get("settings") or {}

            for mode_group in settings.get("availableModes") or []:
                if not isinstance(mode_group, dict):
                    continue

                command_key: str = (
                    mode_group.get("command_key") or mode_group.get("commandKey") or ""
                )

                for raw_setting in mode_group.get("settings") or []:
                    if not isinstance(raw_setting, dict):
                        continue

                    setting_name: str = (
                        raw_setting.get("setting_name")
                        or raw_setting.get("settingName")
                        or ""
                    )
                    setting_value: str = (
                        raw_setting.get("setting_value")
                        or raw_setting.get("settingValue")
                        or ""
                    )

                    label = setting_name
                    for sv in raw_setting.get("setting_values") or []:
                        if isinstance(sv, dict) and sv.get("lang") == "en":
                            synonyms = sv.get("setting_synonym") or []
                            if synonyms:
                                label = synonyms[0]
                            break

                    command = cmd_template.replace(f"{{{command_key}}}", setting_value)

                    presets.append(
                        {
                            "name": label,  # display name, e.g. "Night"
                            "setting_name": setting_name,  # raw key, e.g. "low"
                            "value": setting_value,  # raw value, e.g. "2"
                            "command": command,  # e.g. "tune set mode 2"
                        }
                    )

        _LOGGER.debug("%S: DISCOVERED MODE PRESETS: %S", self._attr_name, presets)
        return presets

    # ── HA properties ─────────────────────────────────────────────────────────

    @property
    def percentage(self) -> int | None:
        """Return current speed as a percentage."""
        speed = (self.coordinator.data or {}).get("speed")
        if speed is None or not self._speed_range:
            return None
        try:
            return ordered_list_item_to_percentage(self._speed_range, int(speed))
        except (ValueError, KeyError):
            return None

    @property
    def preset_mode(self) -> str | None:
        """Return current preset mode label."""
        if not self._mode_presets:
            return None
        mode = (self.coordinator.data or {}).get("mode")
        if mode is None:
            return None
        for preset in self._mode_presets:
            if preset["value"] == str(mode):
                return preset["name"]
        return None

    @property
    def preset_modes(self) -> list[str] | None:
        """Return available preset mode labels."""
        return [p["name"] for p in self._mode_presets] or None

    # ── HA commands ───────────────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut)
    async def async_set_percentage(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_orig(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_1(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage != 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_2(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 1:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_3(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_4(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = None
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_5(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(None, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_6(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, None)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_7(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_8(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, )
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_9(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = None
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_10(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            None, None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_11(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_12(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_13(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(None) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_14(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["XXspeedXX"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_15(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["SPEED"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_16(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) != speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_17(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = None
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_18(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["XXcommandXX"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_19(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["COMMAND"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_20(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            None, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_21(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, None, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_22(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, None
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_23(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_24(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_25(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_26(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = None
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_27(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = None
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_28(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["XXspeedXX"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_29(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["SPEED"] = speed
        self.coordinator.async_set_updated_data(newData)

    # ── HA commands ───────────────────────────────────────────────────────────

    async def xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_30(self, percentage: int) -> None:
        """Set speed by percentage."""
        if percentage == 0:
            await self.async_turn_off()
            return
        if not self._speed_range:
            return
        speed = percentage_to_ordered_list_item(self._speed_range, percentage)
        # Find the matching preset to get the pre-built command
        preset = next(
            (p for p in self._speed_presets if int(p["speed"]) == speed), None
        )
        command = preset["command"] if preset else f"tune set speed {speed}"
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, command
        )
        newData = self.coordinator.data
        newData["speed"] = speed
        self.coordinator.async_set_updated_data(None)

    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut)
    async def async_set_preset_mode(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_orig(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_1(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = None
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_2(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next(None, None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_3(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next(None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_4(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), )
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_5(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["XXnameXX"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_6(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["NAME"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_7(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] != preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_8(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is not None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_9(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                None, self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_10(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", None, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_11(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, None
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_12(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_13(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_14(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_15(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "XX%s: unknown preset mode '%s'XX", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_16(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%S: UNKNOWN PRESET MODE '%S'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_17(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            None, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_18(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, None, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_19(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, None
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_20(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_21(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_22(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_23(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["XXcommandXX"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_24(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["COMMAND"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_25(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = None
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_26(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = None
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_27(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["XXmodeXX"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_28(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["MODE"] = preset["value"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_29(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["XXvalueXX"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_30(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["VALUE"]
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_31(self, preset_mode: str) -> None:
        """Set preset mode by label."""
        preset = next((p for p in self._mode_presets if p["name"] == preset_mode), None)
        if preset is None:
            _LOGGER.warning(
                "%s: unknown preset mode '%s'", self._attr_name, preset_mode
            )
            return
        await self.hass.async_add_executor_job(
            self._api.send_command, self._device_mac, preset["command"]
        )
        newData = self.coordinator.data
        newData["mode"] = preset["value"]
        self.coordinator.async_set_updated_data(None)

    @staticmethod
    @_mutmut_mutated(mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut)
    def _deep_find(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_orig(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_1(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key not in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_2(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(None, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_3(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, None)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_4(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_5(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, )
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_6(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(None, key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_7(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, None)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_8(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(key)

    @staticmethod
    def xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_9(obj: Any, key: str) -> Iterator[Any]:
        """Yield every value for `key` inside a nested dict/list structure."""
        if isinstance(obj, dict):
            if key in obj:
                yield obj[key]
            for value in obj.values():
                yield from DuuxFanAutoDiscovery._deep_find(value, key)
        elif isinstance(obj, list):
            for item in obj:
                yield from DuuxFanAutoDiscovery._deep_find(item, )

mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_10'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_11'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_12'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_13'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_14'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_15'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_16'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_17'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_18'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_19'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_20'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_21'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_22'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_23'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_24'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_25'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_26'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_27'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_28'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ__init____mutmut['xǁDuuxFanAutoDiscoveryǁ__init____mutmut_29'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ__init____mutmut_29 # type: ignore # mutmut generated

mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_10'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_11'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_12'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_13'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_14'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_15'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_16'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_17'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_18'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut['xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_19'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_get_traits__mutmut_19 # type: ignore # mutmut generated

mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_10'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_11'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_12'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_13'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_14'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_15'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_16'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_17'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_18'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_19'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_20'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_21'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_22'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_23'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_24'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_25'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_26'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_27'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_28'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_29'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_30'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_31'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_32'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_33'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_34'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_35'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_36'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_36 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_37'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_37 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_38'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_38 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_39'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_39 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_40'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_40 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_41'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_41 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_42'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_42 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_43'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_43 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_44'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_45'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_46'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_46 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_47'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_47 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_48'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_48 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_49'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_49 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_50'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_50 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_51'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_51 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_52'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_52 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_53'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_53 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_54'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_54 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_55'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_55 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_56'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_56 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_57'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_57 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_58'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_58 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_59'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_59 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_60'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_60 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_61'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_61 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_62'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_62 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_63'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_63 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_64'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_64 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_65'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_65 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_66'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_66 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_67'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_67 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_68'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_68 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_69'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_69 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_70'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_70 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_71'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_71 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_72'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_72 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_73'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_73 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_74'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_74 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_75'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_75 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_76'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_76 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_77'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_77 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_78'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_78 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_79'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_79 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_80'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_80 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_81'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_81 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_82'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_82 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_83'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_83 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_84'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_84 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_85'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_85 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_86'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_86 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_87'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_87 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_88'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_88 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_89'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_89 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_90'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_90 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_91'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_91 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_92'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_92 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_93'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_93 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_94'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_94 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_95'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_95 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_96'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_96 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_97'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_97 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_98'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_98 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_99'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_99 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_100'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_100 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_101'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_101 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_102'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_102 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_103'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_103 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_104'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_104 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_105'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_105 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_106'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_106 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_107'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_107 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_108'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_108 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_109'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_109 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_110'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_110 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_111'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_111 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_112'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_112 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_113'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_113 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_114'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_114 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_115'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_115 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_116'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_116 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_117'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_117 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_118'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_118 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_119'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_119 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_120'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_speeds__mutmut_120 # type: ignore # mutmut generated

mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_10'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_11'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_12'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_13'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_14'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_15'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_16'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_17'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_18'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_19'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_20'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_21'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_22'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_23'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_24'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_25'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_26'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_27'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_28'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_29'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_30'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_31'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_32'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_33'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_34'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_35'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_36'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_36 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_37'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_37 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_38'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_38 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_39'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_39 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_40'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_40 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_41'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_41 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_42'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_42 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_43'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_43 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_44'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_45'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_46'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_46 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_47'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_47 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_48'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_48 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_49'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_49 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_50'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_50 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_51'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_51 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_52'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_52 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_53'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_53 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_54'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_54 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_55'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_55 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_56'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_56 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_57'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_57 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_58'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_58 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_59'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_59 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_60'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_60 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_61'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_61 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_62'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_62 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_63'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_63 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_64'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_64 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_65'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_65 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_66'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_66 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_67'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_67 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_68'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_68 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_69'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_69 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_70'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_70 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_71'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_71 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_72'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_72 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_73'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_73 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_74'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_74 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_75'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_75 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_76'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_76 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_77'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_77 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_78'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_78 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_79'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_79 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_80'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_80 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_81'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_81 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_82'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_82 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_83'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_83 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_84'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_84 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_85'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_85 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_86'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_86 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_87'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_87 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_88'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_88 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_89'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_89 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_90'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_90 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_91'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_91 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_92'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_92 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_93'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_93 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_94'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_94 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_95'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_95 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_96'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_96 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_97'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_97 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_98'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_98 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_99'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_99 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_100'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_100 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_101'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_101 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_102'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_102 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_103'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_103 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_104'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_104 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_105'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_105 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_106'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_106 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_107'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_107 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_108'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_108 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_109'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_109 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_110'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_110 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_111'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_111 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_112'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_112 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_113'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_113 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_114'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_114 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_115'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_115 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_116'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_116 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_117'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_117 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut['xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_118'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_discover_modes__mutmut_118 # type: ignore # mutmut generated

mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_10'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_11'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_12'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_13'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_14'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_15'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_16'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_17'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_18'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_19'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_20'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_21'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_22'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_23'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_24'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_25'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_26'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_27'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_28'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_29'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_30'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_percentage__mutmut_30 # type: ignore # mutmut generated

mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_10'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_11'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_12'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_13'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_14'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_15'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_16'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_17'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_18'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_19'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_20'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_21'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_22'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_23'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_24'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_25'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_26'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_27'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_28'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_29'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_30'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut['xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_31'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁasync_set_preset_mode__mutmut_31 # type: ignore # mutmut generated

mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['_mutmut_orig'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_1'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_2'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_3'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_4'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_5'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_6'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_7'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_8'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut['xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_9'] = DuuxFanAutoDiscovery.xǁDuuxFanAutoDiscoveryǁ_deep_find__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut: MutantDict = {}  # type: ignore


class DuuxAirPurifierFan(DuuxFan):
    """Representation of a Duux air purifier fan."""

    SPEED_RANGE = (1, 4)

    @_mutmut_mutated(mutants_xǁDuuxAirPurifierFanǁ__init____mutmut)
    def __init__(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_orig(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_1(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(None, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_2(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, None, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_3(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, None)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_4(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_5(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_6(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, )
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_7(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = None
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_8(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON & FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_9(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE & FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_10(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED & FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_11(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = None
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_12(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(None)
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_13(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(None, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_14(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, None))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_15(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_16(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, ))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_17(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(2, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_18(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 5))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_19(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = None
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_20(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = None
        self._attr_preset_modes = ["Auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_21(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = None

    def xǁDuuxAirPurifierFanǁ__init____mutmut_22(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["XXAutoXX"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_23(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["auto"]

    def xǁDuuxAirPurifierFanǁ__init____mutmut_24(self, coordinator, api, device):
        """Initialize the fan."""
        super().__init__(coordinator, api, device)
        self._attr_supported_features = (
            FanEntityFeature.SET_SPEED
            | FanEntityFeature.PRESET_MODE
            | FanEntityFeature.TURN_ON
            | FanEntityFeature.TURN_OFF
        )
        SPEED_RANGE_AIR_PURIFIER = list(range(1, 4))
        self._speed_range = SPEED_RANGE_AIR_PURIFIER
        self._attr_speed_count = len(self._speed_range)
        self._attr_preset_modes = ["AUTO"]

    @property
    def is_on(self) -> bool:
        """Return true if fan is on."""
        return (self.coordinator.data or {}).get("power") == 1

    @property
    def percentage(self) -> int | None:
        """Return the current speed percentage."""
        data = self.coordinator.data or {}
        speed = data.get("speed")
        if speed is None:
            return None
        if speed == 0:
            # Auto mode: estimate effective speed from air quality sensors
            # TVOC>1 (Polluted or Harmful) → max speed immediately
            tvoc = data.get("tvoc") or 0
            if tvoc > 1:
                return ranged_value_to_percentage(self.SPEED_RANGE, self.SPEED_RANGE[1])
            # Otherwise derive from AQ: aq=0→speed 1, aq=1→2, aq=2→3, aq≥3→4
            aq = data.get("aq") or 0
            estimated = min(aq + 1, self.SPEED_RANGE[1])
            return ranged_value_to_percentage(self.SPEED_RANGE, estimated)
        return ranged_value_to_percentage(self.SPEED_RANGE, speed)

    @property
    def speed_count(self) -> int:
        """Return the number of speeds the fan supports."""
        return self.SPEED_RANGE[1]

    @property
    def preset_mode(self) -> str | None:
        """Return the current preset mode."""
        speed = (self.coordinator.data or {}).get("speed")
        if speed == 0:
            return "Auto"
        return None

    @_mutmut_mutated(mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut)
    async def async_set_percentage(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_orig(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_1(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage != 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_2(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 1:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_3(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = None

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_4(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(None)

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_5(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(None, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_6(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, None))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_7(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_8(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, ))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_9(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_10(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                None, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_11(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, None, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_12(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, None
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_13(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_14(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_15(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_16(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, False
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_17(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            None, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_18(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, None, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_19(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, None
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_20(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_21(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_22(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_23(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = None
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_24(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = None

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_25(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["XXspeedXX"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_26(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["SPEED"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_27(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 or newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_28(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed != 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_29(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 2 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_30(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get(None) == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_31(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("XXionXX") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_32(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ION") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_33(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") != 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_34(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 2:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_35(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                None, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_36(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, None, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_37(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, None
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_38(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_39(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_40(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_41(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, True
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_42(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = None

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_43(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["XXionXX"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_44(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ION"] = 0

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_45(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 1

        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_46(self, percentage: int) -> None:
        """Set the speed percentage of the fan."""
        if percentage == 0:
            await self.async_turn_off()
            return

        speed = round(percentage_to_ranged_value(self.SPEED_RANGE, percentage))

        # Ensure power is ON before sending speed command
        if not self.is_on:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )

        await self.hass.async_add_executor_job(
            self._api.set_purifier_speed, self._device_mac, speed
        )

        newData = self.coordinator.data
        newData["speed"] = speed

        # Constraint: Ionizer must be OFF if speed is at lowest (1)
        if speed == 1 and newData.get("ion") == 1:
            await self.hass.async_add_executor_job(
                self._api.set_ionizer, self._device_mac, False
            )
            newData["ion"] = 0

        self.coordinator.async_set_updated_data(None)

    @_mutmut_mutated(mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut)
    async def async_set_preset_mode(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_orig(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_1(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode != "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_2(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "XXAutoXX":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_3(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_4(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "AUTO":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_5(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = None
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_6(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_7(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    None, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_8(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, None, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_9(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, None
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_10(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_11(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_12(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_13(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, False
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_14(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = None
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_15(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["XXpowerXX"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_16(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["POWER"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_17(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 2
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_18(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                None, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_19(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, None, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_20(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, None
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_21(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_22(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_23(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_24(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 1
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_25(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = None
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_26(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["XXspeedXX"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_27(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["SPEED"] = 0
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_28(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_29(self, preset_mode: str) -> None:
        """Set the preset mode of the fan."""
        if preset_mode == "Auto":
            newData = self.coordinator.data
            # Ensure power is ON before sending mode command
            if not self.is_on:
                await self.hass.async_add_executor_job(
                    self._api.set_power, self._device_mac, True
                )
                newData["power"] = 1
            await self.hass.async_add_executor_job(
                self._api.set_purifier_speed, self._device_mac, 0
            )
            newData["speed"] = 0
            self.coordinator.async_set_updated_data(None)

    @_mutmut_mutated(mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut)
    async def async_turn_on(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_orig(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_1(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_2(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(None)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_3(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_4(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(None)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_5(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                None, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_6(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, None, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_7(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, None
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_8(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_9(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_10(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_11(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, False
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_12(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = None
            newData["power"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_13(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = None
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_14(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["XXpowerXX"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_15(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["POWER"] = 1
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_16(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 2
            self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_17(
        self,
        percentage: int | None = None,
        preset_mode: str | None = None,
        **kwargs: Any,
    ) -> None:
        """Turn on the fan."""
        if percentage is not None:
            await self.async_set_percentage(percentage)
        elif preset_mode is not None:
            await self.async_set_preset_mode(preset_mode)
        else:
            await self.hass.async_add_executor_job(
                self._api.set_power, self._device_mac, True
            )
            newData = self.coordinator.data
            newData["power"] = 1
            self.coordinator.async_set_updated_data(None)

    @_mutmut_mutated(mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut)
    async def async_turn_off(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_orig(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_1(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            None, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_2(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, None, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_3(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, None
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_4(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_5(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_6(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_7(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, True
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_8(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = None
        newData["power"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_9(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = None
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_10(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["XXpowerXX"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_11(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["POWER"] = 0
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_12(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = 1
        self.coordinator.async_set_updated_data(newData)

    async def xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_13(self, **kwargs: Any) -> None:
        """Turn off the fan."""
        await self.hass.async_add_executor_job(
            self._api.set_power, self._device_mac, False
        )
        newData = self.coordinator.data
        newData["power"] = 0
        self.coordinator.async_set_updated_data(None)

mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['_mutmut_orig'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_1'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_2'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_3'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_4'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_5'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_6'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_7'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_8'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_9'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_10'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_11'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_12'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_13'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_14'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_15'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_16'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_17'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_18'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_19'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_20'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_21'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_22'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_23'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁ__init____mutmut['xǁDuuxAirPurifierFanǁ__init____mutmut_24'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁ__init____mutmut_24 # type: ignore # mutmut generated

mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['_mutmut_orig'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_1'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_2'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_3'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_4'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_5'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_6'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_7'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_8'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_9'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_10'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_11'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_12'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_13'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_14'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_15'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_16'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_17'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_18'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_19'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_20'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_21'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_22'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_23'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_24'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_25'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_26'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_27'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_28'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_29'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_30'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_31'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_32'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_33'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_34'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_35'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_36'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_36 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_37'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_37 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_38'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_38 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_39'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_39 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_40'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_40 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_41'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_41 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_42'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_42 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_43'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_43 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_44'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_45'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut['xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_46'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_percentage__mutmut_46 # type: ignore # mutmut generated

mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['_mutmut_orig'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_1'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_2'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_3'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_4'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_5'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_6'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_7'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_8'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_9'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_10'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_11'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_12'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_13'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_14'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_15'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_16'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_17'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_18'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_19'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_20'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_21'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_22'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_23'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_24'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_25'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_26'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_27'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_28'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut['xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_29'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_set_preset_mode__mutmut_29 # type: ignore # mutmut generated

mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['_mutmut_orig'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_1'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_2'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_3'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_4'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_5'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_6'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_7'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_8'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_9'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_10'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_11'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_12'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_13'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_14'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_15'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_16'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_on__mutmut['xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_17'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_on__mutmut_17 # type: ignore # mutmut generated

mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['_mutmut_orig'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_1'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_2'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_3'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_4'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_5'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_6'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_7'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_8'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_9'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_10'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_11'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_12'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDuuxAirPurifierFanǁasync_turn_off__mutmut['xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_13'] = DuuxAirPurifierFan.xǁDuuxAirPurifierFanǁasync_turn_off__mutmut_13 # type: ignore # mutmut generated
